# CLI overview

The SpecGate CLI provides commands for checking OpenAPI specification readiness, generating suggestions, and reviewing the rules SpecGate enforces.

All commands follow this pattern:

```bash {filename="terminal"}
specgate [command] [file] [flags]
```

Choose a command from the sidebar to view usage, options, and examples.